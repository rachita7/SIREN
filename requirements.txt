# Minimal deps for the analysis/ scripts (plot_layer_probes.py,
# plot_siren_neurons.py, export_topn_neurons.py, clean_stability_intersection.py).
# Versions match environment.yaml.
numpy==2.2.6
pandas==2.3.1
matplotlib==3.10.3
scikit-learn==1.9.0

# Only needed for the Jaccard heatmap part, which unpickles the trained
# probes (they contain torch tensors). Not needed for the CSV counts plot.
torch==2.7.0
